
# import something

# model = nano.models.some_model()
# model.load_state_dict(torch.load('my/path/to.pt')['model'])
# release_version = 'YOLOV1000-MKII'

# freeze(model, target_dir='release/'+release_version, cache_dir='./build', opset=11, clean_build=True)
