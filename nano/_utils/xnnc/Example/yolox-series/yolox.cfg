[path]
input_dir       = model  # Specify directory where input_graph, input_model, input_mean can be found.
output_dir      = output  # Specify directory where the output Xtensa workspace is saved
tmp_dir         = temp  # Specify directory where temporary files are stored
image_dir       = dataset  # Specify directory where image lists and label file is located
annotation_dir  = dataset  # Specify directory where image lists and label file is located

[network]
name = yolox
out_cntrl = output_ctrl.txt
type = caffe protobuf
input_graph = yolov5-mv2-custom.prototxt
input_model = yolov5-mv2.caffemodel
input_mean = meanval.txt
stddev = 25500
channel_order = RGB

[dataset]
type = imagelist  # Type is always imagelist
label_file = voc2012_labels  # Specify label file. If image_dir is specified it will look for label file first in that location and if that fails it will use label_file as
calibration_set = imagelist.txt  # Specify calibration image list file. If image_dir is specified it will look for it first in that location and if that fails it will try calibration_set value
calibration_count = 100  # Number of images to use from calibration image list
validation_set = imagelist.txt  # Specify validation image list file. If image_dir is specified it will look for it first in that location and if that fails it will try validation_set value
validation_count = 100  # Number of images to use from validation image list

[quantization]
accuracy_level = 1
range = 95:99

[system]
tcm_reserve_low = 0x200
tcm_reserve_high = 0x5000
tcm_contiguous = True

[graph]
global_output       = detection_out
coefficient_tables  = asm

[performance]
read_delay     = 200
write_delay    = 200
optimization   = level2

[system]
xtensa_system  = /home/sh/Projects/tensilica/xtensa/XtDevTools/install/tools/RG-2018.9-linux/XtensaTools/config
xtensa_params  =
xtensa_core    = fy01_fpga